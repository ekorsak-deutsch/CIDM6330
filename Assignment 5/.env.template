# Redis Configuration
# Use 'redis' when all services (Django, Celery, Redis) are running in Docker
# Use 'localhost' when Redis is in Docker but Django/Celery are running directly on your computer
REDIS_HOST=redis
REDIS_PORT=6379
REDIS_DB=0

# Django Configuration
DEBUG=True
ALLOWED_HOSTS=localhost,127.0.0.1

# Security - Generate your own SECRET_KEY and add it here
# You can generate a key with:
# python -c "import secrets; print(secrets.token_urlsafe(50))"
SECRET_KEY= 